version: 2

models:
  - name: int_sales_database__customer
    description: '{{ doc("int_sale_database__customer") }}'
    columns:
      - name: customer_id
        description: "Primary key, unique identifier for each customer."
        tests:
          - unique
          - not_null
