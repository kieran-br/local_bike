version: 2

models:
  - name: int_sales_database__customer_favorite_product
    description: '{{ doc("int_sales_database__customer_favorite_product") }}'
    columns:
      - name: customer_id
        description: "Primary key, unique identifier for each user."
        tests:
          - unique
          - not_null